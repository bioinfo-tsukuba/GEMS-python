# 作成中ログ

## 2026-03-04
- `codex/gui-experiment-mvp`ブランチの`git push -u origin codex/gui-experiment-mvp`を実行。
- Push結果は`Everything up-to-date`で、リモート追跡設定を確認。
- GUI最小構成（1 + 2 + 3 + 5 + 7 + 8）実装に着手。
- `gems_python/gui_experiment_builder/`配下にGUIモジュールを新規作成。
- Main画面、Experiment作成ウィザード、状態遷移可視化、入力バリデーションを実装。
- `simulate_one`と`simulate`の実行ボタンをMain画面に追加。
- マシン管理画面（追加、削除、一覧更新）を実装。
- 実験一覧画面（ステータスフィルタ、検索、詳細表示）を実装。
- `main_gui.py`を追加し、GUI起動エントリーポイントを作成。
- `README.md`へGUI起動手順と実装済みMVP機能を追記。
- `tests/test_gui_experiment_builder_validator.py`と`tests/test_gui_experiment_builder_factory.py`を追加。
- テスト時に`tkinter`未導入環境でimport失敗したため、`gems_python/gui_experiment_builder/__init__.py`を遅延importに修正。
- `TaskGroup`の`penalty_type`に`NonePenalty()`を設定し、既存スケジューラ仕様に整合させた。
- `poetry run python -m unittest tests/test_gui_experiment_builder_validator.py tests/test_gui_experiment_builder_factory.py`を実行し、5件すべて成功。
- 追加ファイルに対して`python -m py_compile`を実行し、構文エラーがないことを確認。
